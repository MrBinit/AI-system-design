# Standard production setup (recommended):
# Keep this empty when CloudFront routes /api/* to backend origin.
VITE_API_BASE=

# Optional override only if API is hosted on a separate domain:
# VITE_API_BASE=https://api.example.com
