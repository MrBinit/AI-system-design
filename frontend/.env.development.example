# Local dev backend target used by Vite proxy (/api, /healthz).
VITE_DEV_API_TARGET=http://localhost:8000

# Optional absolute API base for development requests.
# Leave empty to use relative URLs + Vite proxy.
# VITE_API_BASE=http://localhost:8000
